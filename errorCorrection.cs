using System;
using System.Net.Configuration;

#define distance ((n) - (k) + 1)
public static int computeDistance(int n, int k)
{
	return (n - k + 1);
}
public class Class1
{
	public Class1()
	{
        // ref: https://www.youtube.com/watch?v=sRgUrKWiXQs&ab_channel=NoOneAsked
        int main(int [message]){


			// MODULES = [1] + [2]

			// [1] Encoding level: message[n][ECOL] -> message[n - 1][ECOL] - Error correction level (3:27)
			// 00 -> Level H (High) (Up to 30%)
			// 01 -> Level Q (Quartile) (Up to 25%)
			// 10 -> Level M (Medium) (Up to 15%)
			// 11 -> Level L (Low) (Up to 7%)

			// [2] Pattern: message[n-2][ECOL] -> message[n - 4][ECOL] (3:57))

			// MODULES -> XORRED_MODULES -> Original modules = XORRED_MODULES - MODULES


		}
	}
}
